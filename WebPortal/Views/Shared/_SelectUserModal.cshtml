@model List<DataAccess.Models.ApplicationUser>

<div id="select_user_modal" class="modal">
    <div class="modal-box w-11/12 max-w-5xl">
        <div class="">
            <h3 class="font-bold text-lg">Selecteer een bezoeker</h3>
        </div>
        <div class="">
            <table id="datatable" class="table">
                <thead>
                <tr>
                    <th>Gebruikersnaam</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var user in Model)
                {
                    <tr onclick="document.querySelector('#checkbox_@user.UserName').click()">
                        <td class="w-0.5">
                            <input id="checkbox_@user.UserName"
                                   name="userId"
                                   type="radio"
                                   value="@user.Id"
                                   required
                                   onclick="document.querySelector('#render_user_name').innerHTML = '@user.UserName'"
                                   onchange="if (this.checked) document.querySelector('#render_user_name').innerHTML = '@user.UserName'">
                            <label for="checkbox_@user.UserName">
                                @user.UserName
                            </label>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
        <div class="">
            <div class="m-2 flex flex-row justify-between">
                <button type="button" onclick="document.querySelector('#select_user_modal').classList.remove('modal-open')" class="btn btn-success">Selecteer</button>
                <button type="button" onclick="document.querySelector('#select_user_modal').classList.remove('modal-open')" class="btn">Annuleren</button>
            </div>
        </div>
    </div>
</div>